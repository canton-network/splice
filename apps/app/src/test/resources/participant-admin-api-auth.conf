canton.participants.sv1StandaloneParticipant.admin-api {
  auth-services = [
    # used by most of our tests
    {
      type = unsafe-jwt-hmac-256
      secret = "test"
      target-scope = "test-scope"
      privileged = true
      # target-audience = "https://canton.network.global"
      # users = [{
      #   user-id = "admin"
      #   allowed-services = ["*"]
      # }]
    }
  ]
}
